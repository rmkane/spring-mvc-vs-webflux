server:
  port: 8080

spring:
  application:
    name: acme-api-mvc

  datasource:
    url: jdbc:postgresql://localhost:5432/acme_jpa
    username: acme_user
    password: acme_password
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        # Explicitly set dialect for clarity (Hibernate can auto-detect, but explicit is preferred)
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  cache:
    type: caffeine
    cache-names: users
    caffeine:
      spec: >
        expireAfterWrite=5m,
        maximumSize=1000

auth:
  service:
    base-url: https://localhost:8082
    ssl:
      enabled: true
      truststore:
        path: classpath:ssl/auth-service-truststore.jks
        password: changeit
        type: JKS
      keystore:
        path: classpath:ssl/api-mvc-keystore.jks
        password: changeit
        type: JKS

logging:
  level:
    org.acme: DEBUG
    org.springframework.security: DEBUG
    org.springdoc.core.events.SpringDocAppInitializer: ERROR
    org.hibernate.orm.deprecation: ERROR

