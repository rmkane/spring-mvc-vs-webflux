# TLS secret for Ingress server certificate
# This will be created from the server certificate
# For testing, we can use a self-signed cert or the auth-service certificate
# To create: kubectl create secret tls ingress-tls-secret --cert=monitoring/certs/auth-service.crt --key=monitoring/certs/auth-service.key -n acme-ingress
