# Secret containing the CA chain for validating client certificates
# This will be created from the CA chain certificate
# To create: kubectl create secret generic ca-chain-secret --from-file=ca-chain.crt=acme-infrastructure/certs/ca/ca-chain.crt -n acme-ingress
