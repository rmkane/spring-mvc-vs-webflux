apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: acme-ingress
  namespace: acme-apps
  annotations:
    # Enable client certificate authentication
    nginx.ingress.kubernetes.io/auth-tls-verify-client: "on"
    nginx.ingress.kubernetes.io/auth-tls-verify-depth: "2"
    nginx.ingress.kubernetes.io/auth-tls-secret: "acme-apps/ca-chain-secret"
    nginx.ingress.kubernetes.io/auth-tls-verify-client-optional: "true"
    nginx.ingress.kubernetes.io/auth-tls-pass-certificate-to-upstream: "true"
    # Note: ssl-client-subject-dn and ssl-client-issuer-dn headers are automatically
    # set by the Ingress when auth-tls-pass-certificate-to-upstream is "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - acme.local
    secretName: ingress-tls-secret
  rules:
  - host: acme.local
    http:
      paths:
      - path: /test
        pathType: Prefix
        backend:
          service:
            name: test-headers-service
            port:
              number: 8080
      # Route /api/v1/* to the backend API
      # Note: /api/* without v1 goes to the UI (Next.js API routes)
      - path: /api/v1
        pathType: Prefix
        backend:
          service:
            name: api-mvc
            port:
              number: 8080
      - path: /api-webflux/v1
        pathType: Prefix
        backend:
          service:
            name: api-webflux
            port:
              number: 8081
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ui
            port:
              number: 3001
